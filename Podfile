# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Uol' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Uol
  pod 'SwiftyJSON'
  pod 'Alamofire', '~> 4.4'
  pod 'Kingfisher', '~> 3.0'

  target 'UolTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UolUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
